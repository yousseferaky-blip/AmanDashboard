import React from 'react';
import WalletsContent from '../../features/wallets/WalletsContent';

const WalletsPage: React.FC = () => {
  return (
    <div className="min-h-screen bg-gray-50">
      <WalletsContent />
    </div>
  );
};

export default WalletsPage; 